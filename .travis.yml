language: node_js

node_js:
  - "6"

env:
  - NODE_ENV=ci

cache:
  directories:
  - node_modules

install:
  - npm install

script:
  - npm run lint
  - npm run build
  - npm test:no-cache
  - npm run build:prod
  - npm test:no-cache
